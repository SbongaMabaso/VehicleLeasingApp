@model IEnumerable<VehicleLeasingApp.Models.Drivers>

@{
	ViewData["Title"] = "Index";
}

<h1>Manage Drivers</h1>

<p>
	<a class="btn btn-primary" asp-action="Create">Add New Driver</a>
</p>
<table class="table table-bordered table-hover table-responsive-lg">
	<thead style="background-color: #d9edf7;">
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.FullName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.LicenseNumber)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.LicenseExpiry)
			</th>
			<th>Actions</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.FullName)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.LicenseNumber)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.LicenseExpiry)
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.DriverId" title="Edit" class="mx-2">
						<i class="fas fa-edit"></i>
					</a> |
					<a asp-action="Details" asp-route-id="@item.DriverId" title="Details" class="mx-2">
						<i class="fas fa-info-circle"></i>
					</a> |
					<a asp-action="Delete" asp-route-id="@item.DriverId" title="Delete" class="mx-2">
						<i class="fas fa-trash-alt text-danger"></i>
					</a>
				</td>
			</tr>
		}
	</tbody>
</table>

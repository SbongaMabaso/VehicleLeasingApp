@model VehicleLeasingApp.Models.Manufacturers

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit Manufacturer</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ManufacturerId" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group pt-4-3">
                <input type="submit" value="Update Manufacturer" class="btn btn-primary float-end" />
                <a class="btn btn-outline-info float-start" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
